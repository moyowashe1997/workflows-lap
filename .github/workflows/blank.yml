name : Hellow World
on : 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name : Build Step
       run: echo "Building the code as ${{vars.MY_NAME}}"
  test:
   runs-on: ubuntu-latest
   needs: build
   steps:
     - name: Testing Step
       run: echo "Testing the code and my password is ${{secrets.MY_PASSWORD}} "
  deploy:
   runs-on: ubuntu-latest
   needs: test
   steps:
     - name: Deploying Step
       run: echo "Deploying the code"
  
