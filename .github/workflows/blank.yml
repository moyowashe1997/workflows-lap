name : Hellow World
on : 
  push:
    branches:
      - main
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name : Build Step
       run: echo "Building the code"
  test:
   runs-on: ubuntu-latest
   needs: build
   steps:
     - name: Testing Step
       run: echo "Testing the code"
  deploy:
   runs-on: ubuntu-latest
   needs: test
   steps:
     - name: Deploying Step
       run: echo "Deploying the code"
  
